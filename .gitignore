# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.hypothesis/
.coverage
htmlcov/
build/
dist/
.venv/
venv/
.env

# Runtime artefacts
runs/
*.wal

# Node / VitePress
# Unanchored so a stray .vitepress/ at the repo root is ignored too — a dev
# server started from the wrong directory leaves one behind.
node_modules/
**/.vitepress/dist/
**/.vitepress/cache/
